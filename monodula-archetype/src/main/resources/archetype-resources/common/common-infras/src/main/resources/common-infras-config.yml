#set($dollar='$')
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/${dollar}{DB_NAME:dev_db}
    username: ${dollar}{DB_USER:root}
    password: ${dollar}{DB_PASS:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
